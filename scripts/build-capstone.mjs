import crypto from "node:crypto";
import fs from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const sourcePath = path.join(root, "CAPSTONE/source/bus311-capstone.json");
const sourceBytes = await fs.readFile(sourcePath);
const capstone = JSON.parse(sourceBytes.toString("utf8"));
const sourceHash = crypto.createHash("sha256").update(sourceBytes).digest("hex");
const outputDir = path.join(root, "CAPSTONE");

const esc = (value) => String(value ?? "")
  .replaceAll("&", "&amp;")
  .replaceAll("<", "&lt;")
  .replaceAll(">", "&gt;")
  .replaceAll('"', "&quot;");

const materialById = new Map(capstone.materials.map((item) => [item.materialId, item]));
const fileName = (item) => path.basename(item.path);
const publicHref = (item) => `./${fileName(item)}`;
const canvasHref = (item) => `${capstone.hub.publicUrl}${encodeURIComponent(fileName(item))}`;
const stageById = new Map(capstone.hub.stages.map((stage) => [stage.stageId, stage]));
const milestoneDueById = new Map(capstone.milestones.map((item) => [item.milestoneId, item.due]));
const stageOne = stageById.get("S01_SCOPE");
const stageFive = stageById.get("S05_BOARD");
const presentationDates = capstone.presentationSchedule.dates.map((date) => {
  const parsed = new Date(`${date}T12:00:00-05:00`);
  return new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric" }).format(parsed);
}).join(", ");

const resourceGroups = [
  { stageId: "OVERVIEW", title: "Assignment and scoring", eyebrow: "Project overview", ids: ["ASSIGNMENT", "ASSIGNMENT_DOCX", "STUDENT_RUBRIC"] },
  { stageId: "S01_SCOPE", label: "Stage 1", title: "Explore the company and a CFO decision", eyebrow: `Due ${stageById.get("S01_SCOPE").dateLabel}`, due: milestoneDueById.get("M01"), ids: ["M01_MENU", "M01_EXPLORATION"] },
  { stageId: "S02_REVENUE", label: "Stage 2", title: "Build the revenue case", eyebrow: `Due ${stageById.get("S02_REVENUE").dateLabel}`, due: milestoneDueById.get("M02"), ids: ["REVENUE_GUIDE", "REVENUE_WORKBOOK", "HYPOTHESIS_CHECKLIST", "M02_RUBRIC"] },
  { stageId: "S03_VALUE", label: "Stage 3", title: "Model value and scenarios", eyebrow: `Due ${stageById.get("S03_VALUE").dateLabel}`, due: milestoneDueById.get("M03"), ids: ["VALUATION_MODEL"] },
  { stageId: "S04_RED_TEAM", label: "Stage 4", title: "Challenge and revise", eyebrow: `Due ${stageById.get("S04_RED_TEAM").dateLabel}`, due: milestoneDueById.get("M04"), ids: ["AI_GUIDE", "CAPAJ", "RED_TEAM"] },
  { stageId: "S05_BOARD", label: "Stage 5", title: "Submit and present", eyebrow: "PowerPoint due November 30 at 12:30 p.m. ET", due: capstone.finalSubmission.deadline, ids: ["BOARD_TEMPLATE"] },
];
const stageResourceGroups = resourceGroups.filter((group) => group.stageId !== "OVERVIEW");
const colors = ["#355773", "#2f7774", "#c18a42", "#9c4a2b", "#4a7c5e"];

function material(id) {
  const item = materialById.get(id);
  if (!item) throw new Error(`Unknown capstone material: ${id}`);
  return item;
}

function publicResourceGroup(group, className = "resource-group") {
  const links = group.ids.map((id) => {
    const item = material(id);
    const extension = path.extname(item.path).slice(1).toUpperCase();
    return `<a class="resource-card" href="${esc(publicHref(item))}"><span>${esc(extension)}</span><strong>${esc(item.label)}</strong><small>Open or download this student file</small></a>`;
  }).join("");
  return `<section class="${className}"><p class="kicker">${esc(group.eyebrow)}</p><h3>${esc(group.title)}</h3><div class="resource-grid">${links}</div></section>`;
}

function publicStageCards() {
  return capstone.hub.stages.map((stage) => {
    const guide = stage.guideMaterialId ? materialById.get(stage.guideMaterialId) : null;
    return `<article class="stage-card"><p class="stage-label">${esc(stage.label)} · ${esc(stage.points)} points</p><h3>${esc(stage.title)}</h3><dl><div><dt>Outcome</dt><dd>${esc(stage.outcome)}</dd></div><div><dt>Due</dt><dd>${esc(stage.dateLabel)}</dd></div><div><dt>Submit / deliver</dt><dd>${esc(stage.requiredSubmission)}</dd></div></dl><details><summary>Stage details</summary><p>${esc(stage.summary)}</p>${guide ? `<a href="${esc(publicHref(guide))}">Open ${esc(guide.label)}</a>` : ""}</details></article>`;
  }).join("");
}

function publicStageFivePanel() {
  const cards = stageFive.components.map((component) => `<article><p class="component-mode">${esc(component.points)} points · ${esc(component.mode)}</p><h3>${esc(component.title)}</h3><dl><div><dt>Format</dt><dd>${esc(component.format)}</dd></div><div><dt>Timing</dt><dd>${esc(component.timing)}</dd></div><div><dt>Grading evidence</dt><dd>${esc(component.grading)}</dd></div></dl></article>`).join("");
  return `<section class="stage-five-panel" aria-labelledby="stage-five-title"><div class="stage-five-heading"><div><p class="kicker">Stage 5 · 75 points total</p><h2 id="stage-five-title">One upload, one live presentation</h2></div><p>These are the only two assessed Stage 5 components. The PowerPoint is submitted in Canvas; the oral Board presentation is performed live to the class and is not a second uploaded file.</p></div><div class="stage-five-components">${cards}</div><div class="stage-five-callout"><strong>Timing and scoring:</strong> use up to 7 minutes for the decision briefing and up to 3 minutes for live questions. Delivery and question responses are folded into the single 25-point oral-presentation criterion; there is no separately scored question component.</div></section>`;
}

function publicDecisionMenu() {
  const families = capstone.hub.decisionFamilies.map((item) => `<article><h3>${esc(item.family)}</h3><p>${esc(item.summary)}</p><small>Includes: ${item.includes.map(esc).join(" · ")}</small></article>`).join("");
  const allOptions = capstone.hub.decisionMenu.map((item) => `<article><h3>${esc(item.area)}</h3><p>${esc(item.prompt)}</p></article>`).join("");
  return `${families}<details class="all-decisions"><summary>See all 10 decision options</summary><div class="decision-grid all-option-grid">${allOptions}</div></details>`;
}

const stageOneMenu = material("M01_MENU");
const stageOneExploration = material("M01_EXPLORATION");
const assignment = material("ASSIGNMENT");
const studentRubric = material("STUDENT_RUBRIC");
const aiGuide = material("AI_GUIDE");
const capajGuide = material("CAPAJ");
const redTeamRecord = material("RED_TEAM");

const resourceLibrary = `<div class="current-files-intro"><p class="kicker">Current stage files</p><h3 data-current-files-label>Stage 1 files</h3><p>Start with the files for the current stage. The complete maintained student library contains ${capstone.materials.length} files.</p></div><div data-current-stage-files>${stageResourceGroups.map((group, index) => `<div data-current-file-set data-stage-label="${esc(group.label)}" data-stage-due="${esc(group.due)}"${index === 0 ? "" : " hidden"}>${publicResourceGroup(group, "resource-group current-resource-group")}</div>`).join("")}</div><details class="file-library"><summary>Open the complete file library (${capstone.materials.length} files)</summary><div>${resourceGroups.map((group) => publicResourceGroup(group)).join("")}</div></details>`;

const css = `
:root{--ink:#0e1116;--navy:#152536;--paper:#faf8f3;--white:#fff;--text:#1a1f2c;--soft:#5b6472;--border:#d8d3c8;--gold:#c18a42;--terra:#9c4a2b;--steel:#355773;--sage:#4a7c5e;--teal:#2f7774;--shadow:0 18px 50px rgba(21,37,54,.1);--radius:16px;--content:1160px}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--paper);color:var(--text);font:16px/1.55 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}a{color:inherit}:focus-visible{outline:3px solid #e5a84d;outline-offset:4px}.skip{position:fixed;left:12px;top:12px;z-index:20;transform:translateY(-180%);background:#fff;color:#111;padding:12px 16px;border-radius:8px;font-weight:800}.skip:focus{transform:translateY(0)}.hero{background:var(--ink);color:#fff}.hero-inner,.shell{width:min(var(--content),calc(100% - 40px));margin:auto}.hero-inner{padding:24px 0 54px}.topbar{display:flex;justify-content:space-between;align-items:center;gap:20px;min-height:50px;border-bottom:1px solid #313844}.topbar>a{color:#f1c98f;font-weight:900;letter-spacing:.08em;text-decoration:none}.topbar nav{display:flex;flex-wrap:wrap;gap:8px 20px}.topbar nav a{min-height:44px;display:inline-flex;align-items:center;color:#d8dde4;font-weight:750;text-decoration:none}.hero-grid{display:grid;grid-template-columns:minmax(0,1.1fr) minmax(330px,.65fr);gap:clamp(30px,6vw,74px);align-items:end;padding-top:46px}.kicker{margin:0 0 9px;color:var(--terra);font-size:.76rem;font-weight:900;letter-spacing:.11em;text-transform:uppercase}.hero .kicker{color:#e1b574}.hero h1{margin:0;font:400 clamp(3rem,6vw,5.4rem)/.94 Georgia,"Times New Roman",serif;letter-spacing:-.04em}.hero-copy>p:not(.kicker){max-width:720px;color:#d4dae1;font-size:1.08rem}.hero-start{border-left:5px solid var(--gold);border-radius:0 14px 14px 0;background:#171c23;padding:22px}.hero-start h2{margin:0 0 12px;font:400 1.6rem/1.08 Georgia,serif}.hero-start dl,.stage-card dl,.stage-five-components dl{margin:0}.hero-start dl div,.stage-card dl div,.stage-five-components dl div{padding:9px 0;border-top:1px solid var(--border)}.hero-start dl div{border-color:#3c4652}.hero-start dt,.stage-card dt,.stage-five-components dt{color:#e1b574;font-size:.7rem;font-weight:900;letter-spacing:.07em;text-transform:uppercase}.stage-card dt,.stage-five-components dt{color:var(--steel)}.hero-start dd,.stage-card dd,.stage-five-components dd{margin:3px 0 0}.hero-start dd{color:#eef1f4;font-size:.88rem}.start-actions{display:grid;gap:8px;margin-top:14px}.button{min-height:44px;display:inline-flex;align-items:center;justify-content:center;padding:9px 14px;border-radius:8px;background:var(--terra);color:#fff;font-weight:850;text-decoration:none}.button.secondary{background:#fff;color:var(--navy)}.start-secondary{margin:12px 0 0;color:#cbd2da;font-size:.84rem}.start-secondary a{color:#f3dfbd;font-weight:800}.stat-strip{display:grid;grid-template-columns:repeat(4,1fr);width:min(var(--content),calc(100% - 40px));margin:-24px auto 0;background:#fff;border:1px solid var(--border);border-radius:14px;box-shadow:var(--shadow);overflow:hidden}.stat-strip div{padding:18px;border-right:1px solid var(--border)}.stat-strip div:last-child{border-right:0}.stat-strip strong{display:block;color:var(--navy);font-size:1.05rem}.stat-strip span{display:block;margin-top:3px;color:var(--soft);font-size:.82rem}.section{padding:64px 0;border-bottom:1px solid var(--border);scroll-margin-top:18px}.section-heading,.stage-five-heading{display:grid;grid-template-columns:minmax(0,.8fr) minmax(320px,.6fr);gap:44px;align-items:end;margin-bottom:26px}.section-heading h2,.stage-five-heading h2{margin:0;font:400 clamp(2.2rem,4.5vw,3.8rem)/1 Georgia,serif;letter-spacing:-.025em}.section-heading>p,.stage-five-heading>p{margin:0;color:var(--soft)}.stage-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px}.stage-card{min-width:0;padding:18px;background:#fff;border:1px solid var(--border);border-top:5px solid var(--steel);border-radius:14px}.stage-card:nth-child(2){border-top-color:var(--teal)}.stage-card:nth-child(3){border-top-color:var(--gold)}.stage-card:nth-child(4){border-top-color:var(--terra)}.stage-card:nth-child(5){border-top-color:var(--sage)}.stage-label,.component-mode{margin:0;color:var(--terra);font-size:.72rem;font-weight:900;letter-spacing:.07em;text-transform:uppercase}.stage-card h3{margin:10px 0 13px;font-size:1.05rem;line-height:1.22}.stage-card dd{font-size:.84rem;line-height:1.35}.stage-card details{margin-top:8px;padding-top:8px;border-top:1px solid var(--border)}.stage-card summary{min-height:40px;display:flex;align-items:center;color:var(--steel);font-size:.82rem;font-weight:850;cursor:pointer}.stage-card details p{font-size:.84rem;color:var(--soft)}.stage-card details a{color:var(--terra);font-size:.82rem;font-weight:850}.deadline,.stage-five-callout,.guardrail{margin-top:20px;padding:18px 20px;border-left:5px solid var(--gold);border-radius:0 10px 10px 0;background:#fff4df}.stage-five-panel{margin-top:30px;padding:28px;border:1px solid #d6c5ad;border-top:6px solid var(--gold);border-radius:var(--radius);background:#fff;box-shadow:var(--shadow)}.stage-five-heading{margin-bottom:20px}.stage-five-components{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.stage-five-components article{padding:22px;border:1px solid var(--border);border-top:5px solid var(--terra);border-radius:12px;background:#faf8f3}.stage-five-components article:nth-child(2){border-top-color:var(--sage)}.stage-five-components h3{margin:10px 0 16px;color:var(--navy);font:400 1.55rem/1.1 Georgia,serif}.stage-five-components dd{font-size:.9rem}.stage-five-callout{background:#edf3f7;border-left-color:var(--steel)}.board-question{margin-bottom:20px;padding:20px 22px;border-left:5px solid var(--gold);background:#efe8df}.board-question p:last-child{margin:0;color:var(--navy);font:400 1.2rem/1.4 Georgia,serif}.decision-grid,.analysis-grid,.assessment-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.decision-grid article,.analysis-grid article,.assessment-grid article{padding:20px;border:1px solid var(--border);border-radius:12px;background:#fff}.decision-grid h3,.analysis-grid h3,.assessment-grid h3{margin:0;color:var(--navy)}.decision-grid p,.analysis-grid p,.assessment-grid p{color:var(--soft)}.decision-grid small{color:var(--steel);font-weight:750}.all-decisions{grid-column:1/-1;padding:0 18px;border:1px solid var(--border);border-radius:12px;background:#f4efe7}.all-decisions>summary{min-height:54px;display:flex;align-items:center;color:var(--navy);font-weight:850;cursor:pointer}.all-option-grid{margin-bottom:18px}.ai-flow{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:18px}.ai-flow article{padding:18px;border-top:4px solid var(--terra);border-radius:10px;background:#fff}.ai-flow span{color:var(--terra);font-size:.72rem;font-weight:900;text-transform:uppercase}.ai-flow h3{font-size:1rem}.guardrail{border-left-color:var(--terra);background:#f7eee9}.scorecard{margin-top:18px;padding:20px;border-radius:12px;background:var(--navy);color:#fff}.scorecard h3{margin:0 0 12px}.scorecard ul{margin:0;padding:0;list-style:none}.scorecard li{display:flex;justify-content:space-between;gap:16px;padding:9px 0;border-top:1px solid #415266}.scorecard strong{color:#f1c98f}.resource-group{padding:20px 0;border-bottom:1px solid var(--border)}.resource-group h3,.current-files-intro h3{margin:0 0 12px;color:var(--navy);font:400 1.8rem/1.05 Georgia,serif}.resource-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.resource-card{display:flex;flex-direction:column;gap:5px;min-height:104px;padding:15px;border:1px solid var(--border);border-top:4px solid var(--terra);border-radius:10px;background:#fff;text-decoration:none}.resource-card span{color:var(--terra);font-size:.7rem;font-weight:900}.resource-card small{color:var(--soft)}.current-files-intro>p:last-child{color:var(--soft)}.file-library{margin-top:16px;padding:0 18px;border:1px solid var(--border);border-radius:12px;background:#fff}.file-library>summary{min-height:58px;display:flex;align-items:center;color:var(--navy);font-weight:850;cursor:pointer}.file-library>div{border-top:1px solid var(--border)}.submission-actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:22px}.submission-actions .secondary{border:2px solid var(--terra);color:var(--terra)}.faq-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.faq-grid details{padding:0 16px;border:1px solid var(--border);border-radius:10px;background:#fff}.faq-grid summary{min-height:54px;display:flex;align-items:center;color:var(--navy);font-weight:850;cursor:pointer}.faq-grid p{color:var(--soft)}.checklist{columns:2;column-gap:32px;padding-left:22px}.checklist li{break-inside:avoid;margin-bottom:10px;padding-left:4px}footer{padding:28px 20px;background:var(--ink);color:#d4dae1;text-align:center}footer a{color:#f1c98f}[hidden]{display:none!important}@media(max-width:1000px){.hero-grid,.section-heading,.stage-five-heading{grid-template-columns:1fr}.stage-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.stage-card:last-child{grid-column:1/-1}.ai-flow{grid-template-columns:repeat(2,minmax(0,1fr))}.resource-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:700px){.hero-inner,.shell{width:min(100% - 28px,var(--content))}.topbar{align-items:flex-start;flex-direction:column;padding-bottom:8px}.topbar nav{gap:2px 14px}.hero-grid{padding-top:26px}.hero h1{font-size:clamp(2.6rem,12vw,3.5rem)}.hero-start{padding:17px}.stat-strip{grid-template-columns:repeat(2,1fr);width:calc(100% - 28px);margin-top:-18px}.stat-strip div:nth-child(2){border-right:0}.section{padding:48px 0}.stage-grid,.stage-five-components,.decision-grid,.analysis-grid,.assessment-grid,.faq-grid,.resource-grid,.ai-flow{grid-template-columns:1fr}.stage-card:last-child{grid-column:auto}.stage-five-panel{padding:22px 16px}.checklist{columns:1}.submission-actions{display:grid}.button{width:100%}}
`;

const startPanel = `<aside class="hero-start" id="start" aria-labelledby="start-title"><p class="kicker">Start Stage 1</p><h2 id="start-title">${esc(stageOne.title)}</h2><dl><div><dt>What am I doing?</dt><dd>${esc(stageOne.objective)}</dd></div><div><dt>What is due next?</dt><dd><strong>${esc(stageOne.requiredSubmission)}</strong> · ${esc(stageOne.dateLabel)}</dd></div></dl><div class="start-actions"><a class="button" href="${esc(publicHref(stageOneMenu))}">1. Browse potential CFO decisions</a><a class="button secondary" href="${esc(publicHref(stageOneExploration))}">2. Complete the Stage 1 exploration brief</a></div><p class="start-secondary">Details: <a href="${esc(publicHref(assignment))}">assignment</a> · <a href="${esc(publicHref(studentRubric))}">rubric</a></p></aside>`;

const assessmentCards = capstone.rubric.components.map((item) => `<article><p class="component-mode">${esc(item.points)} points</p><h3>${esc(item.title)}</h3><p>${item.componentId === "MILESTONES_MODEL_REVISION" ? "Stages 1-4 retain their approved 5, 8, 8, and 4-point values." : item.componentId === "POWERPOINT_COMPANY_ANALYSIS" ? "Upload the editable company-analysis PowerPoint in Canvas by the file lock." : "Present live to the class; delivery and responses to questions are graded together."}</p></article>`).join("");

const publicHtml = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="Student hub for the BUS311 Individual Company Capstone."><meta name="bus311-capstone-source-version" content="${esc(capstone.meta.sourceVersion)}"><meta name="bus311-capstone-source-sha256" content="${sourceHash}"><title>${esc(capstone.project.title)} · Student Hub</title><style>${css}</style></head><body><a class="skip" href="#main">Skip to capstone content</a><header class="hero"><div class="hero-inner"><div class="topbar"><a href="../">BUS311 course home</a><nav aria-label="Capstone navigation"><a href="#timeline">Stages</a><a href="#stage-five-title">Stage 5</a><a href="#decision">CFO decision</a><a href="#resources">Files</a><a href="#checklist">Checklist</a></nav></div><div class="hero-grid"><div class="hero-copy"><p class="kicker">${esc(capstone.project.subtitle)} · ${esc(capstone.meta.effectiveTerm)}</p><h1>${esc(capstone.project.title)}</h1><p>${esc(capstone.hub.purpose)}</p></div>${startPanel}</div></div></header><div class="stat-strip" aria-label="Project at a glance"><div><strong>100 project points</strong><span>25% of the BUS311 grade</span></div><div><strong>Stages 1-4 · 25 points</strong><span>Approved points and dates unchanged</span></div><div><strong>One Stage 5 upload · 50 points</strong><span>Editable company-analysis .pptx</span></div><div><strong>One live presentation · 25 points</strong><span>Up to 7 + 3 minutes, one oral criterion</span></div></div><main id="main" class="shell"><section class="section" id="timeline"><div class="section-heading"><div><p class="kicker">Five-stage semester plan</p><h2>Build the decision in sequence</h2></div><p>Stage 1 is exploratory. Beginning in Stage 2, each stage adds verified evidence and analysis for the final CFO recommendation.</p></div><div class="stage-grid">${publicStageCards()}</div><div class="deadline"><strong>PowerPoint file lock: ${esc(capstone.finalSubmission.deadlineLabel)}</strong><br>Upload exactly one editable .pptx in Canvas. Oral presentations run ${esc(presentationDates)} on the assigned dates; the live presentation is not a second uploaded file.</div>${publicStageFivePanel()}</section><section class="section" id="decision"><div class="section-heading"><div><p class="kicker">Beginner exploration</p><h2>Notice a decision the CFO might face</h2></div><p>Use these directions as idea starters. For Stage 1, propose one potential decision; you do not need to compare alternatives, prove it, or defend a recommendation yet.</p></div><aside class="board-question"><p class="kicker">Your Board question</p><p>${esc(capstone.hub.centralQuestion)}</p></aside><div class="decision-grid">${publicDecisionMenu()}</div><div class="deadline"><strong>Stage 1 boundary:</strong> record one concrete FactSet learning and name the screen, report, or feature used. Formal citations, verification, an evidence register, hypothesis validation, alternatives, proof, and a formal approval gate are not required yet.</div></section><section class="section" id="evidence"><div class="section-heading"><div><p class="kicker">Revenue, evidence, and judgment</p><h2>Make every later conclusion traceable</h2></div><p>Beginning in Stage 2, use SEC filings and permitted FactSet evidence to diagnose the operating business, then use Excel to test what the decision could mean for value.</p></div><div class="analysis-grid"><article><h3>Revenue engine</h3><p>Map products, segments, customers, channels, pricing, and recurring or transactional revenue. Analyze three to five comparable years and isolate company-relevant drivers.</p></article><article><h3>Model and scenarios</h3><p>Separate inputs, sources, calculations, outputs, checks, scenarios, and sensitivities. Reconcile every Stage 5 valuation figure to the assessed Excel model.</p></article><article><h3>Decision and implementation</h3><p>State one specific CFO recommendation, compare credible alternatives, and specify actions, owners, timing, resources, indicators, triggers, risks, and mitigations.</p></article><article><h3>Accessible evidence</h3><p>Use readable editable visuals with units, periods, definitions, compact SEC/FactSet/model source notes, limitations, and the verified AI-use disclosure.</p></article></div><h3>Four bounded AI checkpoints after Stage 1</h3><div class="ai-flow">${capstone.aiCheckpoints.map((item, index) => `<article><span>Checkpoint ${index + 1}</span><h3>${esc(item.title)}</h3><p>${esc(item.purpose)}</p></article>`).join("")}</div><div class="guardrail"><strong>AI is not a source.</strong> Verify every retained claim or calculation with SEC, FactSet, Excel, or course concepts. Never upload raw licensed, private, or instructor-only material.</div></section><section class="section" id="assessment"><div class="section-heading"><div><p class="kicker">Assessment</p><h2>100 points, with exactly two Stage 5 criteria</h2></div><p>Stages 1-4 remain 25 points total. Stage 5 contributes 75 points through the one uploaded PowerPoint and one live oral presentation.</p></div><div class="assessment-grid">${assessmentCards}</div><aside class="scorecard"><h3>Project point check</h3><ul>${capstone.rubric.components.map((item) => `<li><span>${esc(item.title)}</span><strong>${esc(item.points)}</strong></li>`).join("")}<li><span>Total</span><strong>100</strong></li></ul></aside></section><section class="section" id="resources"><div class="section-heading"><div><p class="kicker">Rubrics, templates, and milestone files</p><h2>Use the maintained student package</h2></div><p>Open the assignment first, then use the files associated with the stage you are completing. Canvas remains authoritative for submission links and presentation order.</p></div>${resourceLibrary}<div class="submission-actions"><a class="button" href="${esc(capstone.hub.canvasProjectPageUrl)}">Open the Canvas project page</a><a class="button secondary" href="${esc(capstone.hub.canvasAssignmentsUrl)}">Open Canvas assignments</a></div></section><section class="section" id="faq"><div class="section-heading"><div><p class="kicker">Frequently asked questions</p><h2>Know the boundaries</h2></div><p>These answers summarize the approved project contract. Canvas controls any later instructor-announced change.</p></div><div class="faq-grid">${capstone.hub.faqs.map((item) => `<details><summary>${esc(item.question)}</summary><p>${esc(item.answer)}</p></details>`).join("")}</div></section><section class="section" id="checklist"><div class="section-heading"><div><p class="kicker">Student progress checklist</p><h2>Check the full decision chain</h2></div><p>Use this before each milestone and once more before the Stage 5 PowerPoint file lock and live presentation.</p></div><ol class="checklist">${capstone.hub.progressChecklist.map((item) => `<li>${esc(item)}</li>`).join("")}</ol></section></main><footer><p><strong>BUS311 Corporate Finance</strong> · ${esc(capstone.meta.effectiveTerm)} · <a href="../">Return to the course hub</a></p><p>Canvas is the authority for live submission links, oral-presentation order, and instructor-announced changes.</p></footer><script>(()=>{const sets=[...document.querySelectorAll('[data-current-file-set]')];if(!sets.length)return;const now=Date.now();const current=sets.find((set)=>Date.parse(set.dataset.stageDue)>=now)||sets[sets.length-1];sets.forEach((set)=>{set.hidden=set!==current});const label=document.querySelector('[data-current-files-label]');if(label)label.textContent=current.dataset.stageLabel+' files';})();</script></body></html>`;

const canvasButton = (href, label, color = "#9c4a2b") => `<a style="display:inline-block;box-sizing:border-box;min-height:44px;margin:0 8px 8px 0;padding:11px 15px;background-color:${color};color:#ffffff;font-weight:bold;text-decoration:none;border-radius:7px;" href="${esc(href)}" target="_blank" rel="noopener">${esc(label)}</a>`;
const canvasCard = (title, body, color = "#355773") => `<article style="flex:1 1 250px;box-sizing:border-box;padding:18px;background-color:#ffffff;border:1px solid #d8d3c8;border-top:5px solid ${color};border-radius:9px;"><h3 style="margin:0 0 8px;color:#152536;font-size:18px;line-height:1.25;">${esc(title)}</h3><p style="margin:0;color:#4f5965;line-height:1.5;">${esc(body)}</p></article>`;
const canvasStageCards = capstone.hub.stages.map((stage, index) => canvasCard(`${stage.label}: ${stage.title}`, `${stage.dateLabel} · ${stage.points} points. ${stage.summary}`, colors[index])).join("");
const canvasStageFiveComponents = stageFive.components.map((component, index) => `<article style="flex:1 1 330px;box-sizing:border-box;padding:20px;background-color:#ffffff;border:1px solid #d8d3c8;border-top:6px solid ${index ? "#4a7c5e" : "#9c4a2b"};border-radius:10px;"><p style="margin:0 0 5px;color:#9c4a2b;font-size:12px;font-weight:bold;letter-spacing:.07em;text-transform:uppercase;">${esc(component.points)} points · ${esc(component.mode)}</p><h3 style="margin:0 0 12px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:23px;font-weight:normal;">${esc(component.title)}</h3><p style="margin:0 0 8px;"><strong>Format:</strong> ${esc(component.format)}</p><p style="margin:0 0 8px;"><strong>Timing:</strong> ${esc(component.timing)}</p><p style="margin:0;color:#4f5965;"><strong>Grading evidence:</strong> ${esc(component.grading)}</p></article>`).join("");
const canvasResources = resourceGroups.map((group) => `<section style="margin:0 0 20px;padding:0 0 20px;border-bottom:1px solid #d8d3c8;"><p style="margin:0 0 4px;color:#9c4a2b;font-size:12px;font-weight:bold;letter-spacing:.07em;text-transform:uppercase;">${esc(group.eyebrow)}</p><h3 style="margin:0 0 10px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:22px;font-weight:normal;">${esc(group.title)}</h3><div style="display:flex;flex-wrap:wrap;gap:8px;">${group.ids.map((id) => { const item = material(id); return canvasButton(canvasHref(item), item.label, "#355773"); }).join("")}</div></section>`).join("");

const canvasHtml = `<div style="max-width:1100px;margin:0 auto;padding:8px 14px 32px;color:#1a1f2c;font-family:Arial,Helvetica,sans-serif;line-height:1.55;">
  <div style="padding:28px;background-color:#152536;color:#ffffff;border-radius:12px;"><p style="margin:0 0 6px;color:#e1b574;font-size:13px;font-weight:bold;letter-spacing:.08em;text-transform:uppercase;">BUS311 · ${esc(capstone.meta.effectiveTerm)}</p><h1 style="margin:0 0 12px;font-family:Georgia,'Times New Roman',serif;font-size:36px;font-weight:normal;line-height:1.05;">${esc(capstone.project.title)}</h1><p style="margin:0;color:#e6ebef;">${esc(capstone.hub.purpose)}</p></div>
  <section style="margin:24px 0;padding:20px;background-color:#fff4df;border-left:6px solid #c18a42;border-radius:8px;"><h2 style="margin:0 0 8px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:27px;font-weight:normal;">Start here</h2><p style="margin:0 0 12px;"><strong>${esc(stageOne.title)}</strong> · ${esc(stageOne.dateLabel)}</p>${canvasButton(canvasHref(stageOneMenu), "1. Browse potential CFO decisions")}${canvasButton(canvasHref(stageOneExploration), "2. Complete the Stage 1 exploration brief", "#355773")}</section>
  <section style="margin:0 0 28px;"><h2 style="margin:0 0 14px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">Five-stage plan</h2><div style="display:flex;flex-wrap:wrap;gap:12px;">${canvasStageCards}</div></section>
  <section style="margin:0 0 30px;padding:22px;background-color:#faf8f3;border:1px solid #d6c5ad;border-top:7px solid #c18a42;border-radius:10px;"><p style="margin:0 0 5px;color:#9c4a2b;font-size:12px;font-weight:bold;letter-spacing:.08em;text-transform:uppercase;">Stage 5 · 75 points total</p><h2 style="margin:0 0 10px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">One upload, one live presentation</h2><p style="margin:0 0 16px;">These are the only two assessed Stage 5 components. Upload the PowerPoint in Canvas; perform the oral Board presentation live to the class.</p><div style="display:flex;flex-wrap:wrap;gap:14px;">${canvasStageFiveComponents}</div><div style="margin:16px 0 0;padding:16px;background-color:#edf3f7;border-left:5px solid #355773;border-radius:6px;"><strong>Timing and scoring:</strong> use up to 7 minutes for the decision briefing and up to 3 minutes for live questions. Delivery and question responses are folded into the single 25-point oral criterion; there is no separately scored question component.</div></section>
  <section style="margin:0 0 28px;"><h2 style="margin:0 0 10px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">Submission versus live presentation</h2><div style="padding:18px;background-color:#f7eee9;border-left:6px solid #9c4a2b;border-radius:8px;"><p style="margin:0 0 8px;"><strong>Upload:</strong> exactly one editable <code>.pptx</code>, named <code>${esc(capstone.finalSubmission.files[0])}</code>, by ${esc(capstone.finalSubmission.deadlineLabel)}.</p><p style="margin:0;"><strong>Live:</strong> present the submitted deck to the class on the assigned date. Canvas contains the presentation order. The oral presentation is not a second file.</p></div></section>
  <section style="margin:0 0 28px;"><h2 style="margin:0 0 14px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">Assessment and final requirements</h2><div style="display:flex;flex-wrap:wrap;gap:12px;">${capstone.rubric.components.map((item, index) => canvasCard(`${item.points} points`, item.title, colors[index])).join("")}</div><p style="margin:14px 0 0;"><strong>Total:</strong> 100 project points. Stages 1-4 remain 5, 8, 8, and 4 points; Stage 5 is 50 plus 25.</p></section>
  <section style="margin:0 0 28px;"><h2 style="margin:0 0 14px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">Current student files</h2><details style="padding:0 18px;background-color:#ffffff;border:1px solid #d8d3c8;border-radius:8px;"><summary style="min-height:54px;display:flex;align-items:center;color:#152536;font-weight:bold;cursor:pointer;">Open the complete file library (${capstone.materials.length} files)</summary><div style="padding-top:16px;border-top:1px solid #d8d3c8;">${canvasResources}</div></details></section>
  <section style="margin:0 0 28px;"><h2 style="margin:0 0 14px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">Frequently asked questions</h2>${capstone.hub.faqs.map((item) => `<details style="margin:0 0 9px;padding:0 16px;background-color:#ffffff;border:1px solid #d8d3c8;border-radius:8px;"><summary style="min-height:52px;display:flex;align-items:center;color:#152536;font-weight:bold;cursor:pointer;">${esc(item.question)}</summary><p style="margin:0 0 14px;color:#4f5965;">${esc(item.answer)}</p></details>`).join("")}</section>
  <section style="margin:0 0 28px;"><h2 style="margin:0 0 14px;color:#152536;font-family:Georgia,'Times New Roman',serif;font-size:29px;font-weight:normal;">Student progress checklist</h2><ol style="padding-left:24px;">${capstone.hub.progressChecklist.map((item) => `<li style="margin:0 0 9px;">${esc(item)}</li>`).join("")}</ol></section>
  <div style="padding:18px;background-color:#152536;color:#ffffff;border-radius:8px;">${canvasButton(capstone.hub.canvasAssignmentsUrl, "Open Canvas assignments")}${canvasButton(capstone.hub.publicUrl, "Open the public project hub", "#355773")}<p style="margin:8px 0 0;color:#d9e0e6;">Canvas is authoritative for the upload link, presentation order, and later instructor-announced changes.</p></div>
</div>`;

const stageFiveMarkdown = stageFive.components.map((component) => `### ${component.title} — ${component.points} points\n\n- **Mode:** ${component.mode}\n- **Format:** ${component.format}\n- **Timing:** ${component.timing}\n- **Grading evidence:** ${component.grading}`).join("\n\n");
const rubricMarkdown = capstone.rubric.criteria.map((criterion) => `- **${criterion.title} (${criterion.points} points):** Complete — ${criterion.performanceLevels.COMPLETE.description} Developing — ${criterion.performanceLevels.DEVELOPING.description} Not demonstrated — ${criterion.performanceLevels.NOT_DEMONSTRATED.description}`).join("\n");
const assignmentMarkdown = `# ${capstone.project.title}\n\n**Status:** Approved student release | **Source:** ${capstone.meta.maintainedSource} | **Version:** ${capstone.meta.sourceVersion} | **SHA-256:** ${sourceHash}\n\n## Stage 1: ${stageOne.title}\n\n**Objective:** ${stageOne.objective}\n\n**Outcome:** ${stageOne.outcome}\n\n**Submit:** ${stageOne.requiredSubmission}.\n\n**Due:** ${capstone.milestones.find((item) => item.milestoneId === "M01").dueLabel}.\n\n**Required FactSet learning:** ${stageOne.factSetLearning}\n\n**Not required yet:** ${stageOne.notRequiredYet.join("; ")}.\n\n## Stage 5: exactly two assessed components — 75 points\n\n${stageFiveMarkdown}\n\n### Upload versus live presentation\n\nUpload exactly one editable PowerPoint company-analysis project file in Canvas by **${capstone.finalSubmission.deadlineLabel}**. Required filename: \`${capstone.finalSubmission.files[0]}\`. The oral Board presentation happens live to the class on the assigned date and is not a second uploaded file. Use up to ${capstone.presentationSchedule.presentationMinutes} minutes for the decision briefing and up to ${capstone.presentationSchedule.questionMinutes} minutes for live questions; both are scored together in the single 25-point oral criterion.\n\n## Project scoring\n\n- Stages 1-4: 5 + 8 + 8 + 4 = 25 points.\n- PowerPoint company-analysis project submission: 50 points.\n- oral Board presentation to the class: 25 points.\n- **Total: 100 points.**\n\n## Requirements\n\n${capstone.requirements.map((item) => `- ${item.text}`).join("\n")}\n\n## Milestones\n\n${capstone.milestones.map((item) => `- **${item.title} (${item.points} points):** ${item.dueLabel}`).join("\n")}\n\n## Rubric evidence\n\n${rubricMarkdown}\n\n## Policies\n\n- ${capstone.policies.stageOneExploration}\n- ${capstone.policies.milestoneDeadlines}\n- ${capstone.policies.factSetPermission}\n- ${capstone.policies.sourceLabels}\n- ${capstone.policies.verifiedAIJudgment}\n- ${capstone.policies.lateWork}\n- ${capstone.policies.revision}\n- ${capstone.policies.finalFileLock}\n- ${capstone.policies.submissionLocation}\n- ${capstone.policies.absence}\n`;

await fs.mkdir(outputDir, { recursive: true });
await fs.writeFile(path.join(outputDir, "index.html"), publicHtml);
await fs.writeFile(path.join(outputDir, "bus311-capstone-canvas.html"), canvasHtml);
await fs.writeFile(path.join(outputDir, "bus311-capstone-assignment.md"), assignmentMarkdown);

const artifactPaths = capstone.materials.map((item) => item.path).filter((item) => item !== "CAPSTONE/bus311-capstone-assignment.pdf");
const artifacts = [];
for (const relative of artifactPaths) {
  const bytes = await fs.readFile(path.join(root, relative));
  artifacts.push({ path: relative, sha256: crypto.createHash("sha256").update(bytes).digest("hex") });
}
for (const [relative, contents] of [
  ["CAPSTONE/index.html", publicHtml],
  ["CAPSTONE/bus311-capstone-canvas.html", canvasHtml],
  ["CAPSTONE/bus311-capstone-assignment.md", assignmentMarkdown],
]) {
  artifacts.push({ path: relative, sha256: crypto.createHash("sha256").update(contents).digest("hex") });
}
await fs.writeFile(path.join(outputDir, "output-provenance.json"), JSON.stringify({ sourceVersion: capstone.meta.sourceVersion, sourceSha256: sourceHash, generatedAt: "2026-08-11", artifacts }, null, 2) + "\n");

await import("./build-index.mjs");
console.log(`Built BUS311 capstone ${capstone.meta.sourceVersion} (${sourceHash.slice(0, 12)})`);
