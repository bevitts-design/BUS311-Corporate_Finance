import fs from 'node:fs/promises';
import path from 'node:path';
import crypto from 'node:crypto';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const bytes=await fs.readFile(path.join(root,'CAPSTONE/source/bus311-capstone.json'));
const d=JSON.parse(bytes), out=path.join(root,'CAPSTONE');
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paragraph=t=>`<p>${esc(t)}</p>`;
const list=items=>`<ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;
function body(canvas=false){
 const href=id=>{const m=d.materials.find(x=>x.materialId===id);return (canvas?d.hub.publicUrl:'./')+path.basename(m.path)};
 const link=(id,label)=>`<a href="${esc(href(id))}">${esc(label)}</a>`;
 return `<header><p>BUS311 Corporate Finance · Fall 2026 · Individual project</p><h1>One company. One financial decision.</h1>${paragraph(d.hub.purpose)}${paragraph(d.project.companyEligibility)}<p><strong>100 project points · 25% of your course grade</strong></p><p>${link('PROJECT_WORKBOOK','Download the continuing workbook')} &nbsp; ${link('BOARD_TEMPLATE','Download the six-slide template')}</p></header>
 <section id="pathway"><h2>Your project pathway</h2>${d.hub.stages.map(s=>`<article><h3>${esc(s.label)}: ${esc(s.title)}</h3><p><strong>${esc(s.dateLabel)} · ${s.points} points</strong></p><p><strong>Do:</strong> ${esc(s.work)}</p><p><strong>Submit:</strong> ${esc(s.submit)}</p><p><strong>Grading:</strong> ${esc(s.grade)}</p><p>${esc(s.hours)}</p></article>`).join('')}<p><a href="${esc(d.hub.canvasAssignmentsUrl)}">Open Canvas assignments</a> for the upload locations and presentation order.</p></section>
 <section id="scope"><h2>Keep the research manageable</h2><p>Three years of revenue. Two drivers. One peer comparison. Two risks. One forecast claim.</p><p>Explain how these facts affect your decision. Additional research is optional and earns credit only through stronger evidence in the published criteria.</p><details><summary>Missing data, sources, and FactSet help</summary>${paragraph(d.policies.missingEvidence)}${paragraph(d.policies.sources)}<p><a href="${esc(d.hub.factSetSetupUrl)}">FactSet activation and Excel setup</a></p></details></section>
 <section id="model"><h2>Choose one decision path</h2>${d.decisionPaths.map(x=>`<article><h3>${esc(x.title)}</h3>${paragraph(x.question)}<p><strong>Method:</strong> ${esc(x.method)}</p><details><summary>Inputs and scope</summary>${paragraph(x.inputs)}${paragraph(x.boundary)}</details></article>`).join('')}<p>Both paths use the Model tab. Compare incremental cash flows with the status quo, whose incremental NPV is zero. The template provides the calculations; you supply and explain the assumptions.</p><details><summary>Model preparation and feedback</summary>${paragraph(d.feedback.support)}${paragraph('Feedback target: '+d.feedback.target+'.')}${paragraph(d.feedback.revision)}${paragraph(d.feedback.finalModel)}</details></section>
 <section id="challenge"><h2>Check one meaningful challenge</h2>${paragraph(d.policies.challenge)}${list(d.challengePrompts)}<p>Use the Revision tab. No separate AI log, prompt template, checklist, or revision submission is required.</p></section>
 <section id="presentation"><h2>Your six-slide decision briefing</h2><ol>${d.slideOutline.map(s=>`<li><strong>${esc(s.title)}</strong> ${esc(s.prompt)} <em>${esc(s.evidence)}</em></li>`).join('')}</ol><p>Use six core slides plus an optional supporting appendix. Present for up to seven minutes, then answer questions for up to three minutes. The template notes offer a suggested timing plan.</p><p>Upload <strong>${esc(d.finalSubmission.files[0])}</strong> by ${esc(d.finalSubmission.deadlineLabel)}. Present that same file on your assigned date.</p></section>
 <section id="rubric"><h2>How the 100 points are earned</h2><p>Workbook milestones: 25. PowerPoint: 50. Oral Presentation: 25.</p>${d.rubric.criteria.map(c=>`<p><strong>${esc(c.title)} · ${c.points} points</strong><br>${esc(c.description)}</p>`).join('')}${paragraph(d.rubric.partialCredit)}</section>
 <details id="example"><summary>Annotated example: what sufficient work looks like</summary><h2>${esc(d.example.company)}</h2>${paragraph(d.example.source)}${paragraph(d.example.decision)}${paragraph(d.example.claim)}${list(d.example.annotations)}<p>Open the Example tab in the continuing workbook to see the calculations and a justified recommendation. The example is teaching support, not an additional submission.</p></details>
 <details id="policies"><summary>Submission, revision, and research policies</summary>${Object.values(d.policies).map(paragraph).join('')}</details>
 <footer><p>Optional copies: ${link('ASSIGNMENT','Printable brief (PDF)')} · ${link('ASSIGNMENT_DOCX','Editable brief (Word)')}</p><p>Canvas is authoritative for live upload links, presentation order, and later instructor-announced changes.</p></footer>`;
}
const css=`:root{color-scheme:light}*{box-sizing:border-box}body{margin:0;background:#faf8f3;color:#152536;font:17px/1.6 Arial,sans-serif;overflow-wrap:anywhere}main{max-width:960px;margin:auto;padding:32px 24px}header{border-bottom:5px solid #9c4a2b;padding-bottom:22px}h1{font:46px/1.12 Georgia,serif;max-width:750px}h2{font:30px/1.25 Georgia,serif;margin-top:38px}h3{font-size:21px;margin:0 0 8px}a{color:#813d24;text-underline-offset:3px}a:focus-visible,summary:focus-visible{outline:3px solid #355773;outline-offset:4px}section{margin:30px 0}article{background:#fff;padding:22px;margin:16px 0;border-left:4px solid #355773}article p{margin:8px 0}details{border-top:1px solid #d8d3c8;padding:16px 0}summary{font-weight:bold;cursor:pointer;min-height:44px}li{margin:10px 0}footer{margin-top:36px;border-top:1px solid #d8d3c8;font-size:15px}.skip{position:absolute;left:-10000px}.skip:focus{left:20px;top:10px;background:white;padding:10px}@media(max-width:600px){main{padding:22px 16px}h1{font-size:35px}h2{font-size:26px}article{padding:16px}}@media print{body{background:white}main{max-width:none}}`;
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="bus311-source-sha256" content="${crypto.createHash('sha256').update(bytes).digest('hex')}"><title>BUS311 Company Capstone</title><style>${css}</style></head><body><a class="skip" href="#pathway">Skip to project pathway</a><main>${body()}</main></body></html>`;
const styles={header:'border-bottom:4px solid #9c4a2b;padding-bottom:18px;',h1:'color:#152536;font-size:34px;line-height:1.2;',h2:'color:#152536;font-size:27px;margin-top:30px;',h3:'color:#152536;font-size:21px;',article:'background:#faf8f3;border-left:4px solid #355773;padding:18px;margin:16px 0;',summary:'font-weight:bold;min-height:44px;padding:12px 0;',a:'color:#813d24;text-decoration:underline;',li:'margin:10px 0;'};
const canvas='<div style="max-width:960px;margin:auto;font-family:Arial,sans-serif;font-size:16px;line-height:1.6;color:#152536;overflow-wrap:anywhere;">'+body(true).replace(/<(header|h1|h2|h3|article|summary|a|li)(?=[ >])/g,(_,t)=>`<${t} style="${styles[t]}"`)+'</div>';
const md=`# ${d.project.title}\n\n${d.hub.purpose}\n\n100 project points; 25% of the course grade.\n\n`+d.hub.stages.map(s=>`## ${s.label}: ${s.title}\n\n**Due:** ${s.dateLabel}. **Points:** ${s.points}.\n\n**Do:** ${s.work}\n\n**Submit:** ${s.submit}\n\n**Grading:** ${s.grade}\n\n${s.hours}\n`).join('\n')+`\n## Decision paths\n\n`+d.decisionPaths.map(x=>`### ${x.title}\n\n${x.question}\n\n${x.method} ${x.inputs}\n\n${x.boundary}\n`).join('\n')+`\n## Feedback and revision\n\nFeedback target: ${d.feedback.target}. ${d.feedback.revision}\n\n${d.feedback.finalModel}\n\n## Six-slide presentation\n\n`+d.slideOutline.map((s,i)=>`${i+1}. **${s.title}** ${s.prompt} ${s.evidence}`).join('\n')+`\n\n## Grading\n\n`+d.rubric.criteria.map(c=>`- **${c.title} (${c.points}):** ${c.description}`).join('\n')+`\n\n${d.rubric.partialCredit}\n\n## Policies\n\n`+Object.values(d.policies).map(t=>'- '+t).join('\n')+'\n';
await fs.writeFile(path.join(out,'index.html'),html);
await fs.writeFile(path.join(out,'bus311-capstone-canvas.html'),canvas);
await fs.writeFile(path.join(out,'bus311-capstone-assignment.md'),md);
const artifacts=[];
for(const rel of [...d.materials.map(m=>m.path),'CAPSTONE/index.html','CAPSTONE/bus311-capstone-canvas.html','CAPSTONE/bus311-capstone-assignment.md']){
 try{const b=await fs.readFile(path.join(root,rel));artifacts.push({path:rel,sha256:crypto.createHash('sha256').update(b).digest('hex')});}catch(e){if(e.code!=='ENOENT')throw e;}
}
await fs.writeFile(path.join(out,'output-provenance.json'),JSON.stringify({sourceVersion:d.meta.sourceVersion,sourceSha256:crypto.createHash('sha256').update(bytes).digest('hex'),artifacts},null,2)+'\n');
await import('./build-index.mjs');
console.log('Built capstone pathway, Canvas fragment, assignment, and homepage.');
